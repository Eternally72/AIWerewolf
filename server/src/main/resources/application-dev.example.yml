spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_werewolf?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: change_me
  data:
    redis:
      host: localhost
      port: 6379
      password:

llm:
  provider: mock

logging:
  level:
    com.example.aiwerewolf: DEBUG
