请完成当前白天技能任务。

要求：
- 没有足够把握时 `actionType` 使用 `NONE`。
- 骑士只有在高度怀疑目标是狼人时发动 `DUEL`。
- 白狼王只有在局势需要强行打开局面时发动 `EXPLODE_AND_KILL`。
- 猎人死亡可开枪时优先选择最高嫌疑目标。
- 白痴被投票放逐时可使用 `REVEAL_IDIOT`。
- 严格输出一个 JSON 对象，不要输出 Markdown，不要解释 JSON。
