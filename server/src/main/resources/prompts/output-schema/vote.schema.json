{
  "type": "object",
  "required": ["targetPlayerRef", "reason", "confidence"],
  "properties": {
    "targetPlayerRef": {"type": "string", "pattern": "^seat-[0-9]+$"},
    "reason": {"type": "string"},
    "confidence": {"type": "number", "minimum": 0, "maximum": 1}
  }
}
