{
  "type": "object",
  "required": ["actionType", "targetPlayerId", "secondaryTargetPlayerId", "reason"],
  "properties": {
    "actionType": {"type": "string", "enum": ["DUEL", "EXPLODE_AND_KILL", "SHOOT", "REVEAL_IDIOT", "NONE"]},
    "targetPlayerId": {"type": ["string", "null"]},
    "secondaryTargetPlayerId": {"type": ["string", "null"]},
    "reason": {"type": "string"}
  }
}
