{
  "type": "object",
  "required": ["actionType", "targetPlayerRef", "secondaryTargetPlayerRef", "reason"],
  "properties": {
    "actionType": {"type": "string", "enum": ["DUEL", "EXPLODE_AND_KILL", "SHOOT", "REVEAL_IDIOT", "NONE"]},
    "targetPlayerRef": {"type": ["string", "null"]},
    "secondaryTargetPlayerRef": {"type": ["string", "null"]},
    "reason": {"type": "string"}
  }
}
